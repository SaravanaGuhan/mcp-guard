[pytest]
testpaths = tests
addopts =
    -q
    --strict-markers
    --cov=mcp_guard
    --cov-report=term-missing:skip-covered
    --cov-fail-under=70
filterwarnings =
    ignore::DeprecationWarning
