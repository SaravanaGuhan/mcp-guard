# Runtime dependencies for MCP Guard
requests>=2.31.0        # repository acquisition, OSV API
cvss>=3.0               # CVSS v4.0 vector parsing and scoring
PyYAML>=6.0             # yarn.lock / pnpm-lock parsing helpers
tomli>=2.0.0; python_version < "3.11"   # pyproject parsing on 3.10

# Development / test
pytest>=7.0.0
pytest-asyncio>=0.21.0
pytest-cov>=4.0.0
jsonschema>=4.0.0       # SARIF schema validation in tests
